{% block body %}
<div class="container">
        <h2>Réinitialisation du mot de passe</h2>
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.password, {'attr': {'placeholder': 'Mot de passe', 'required': 'required'}}) }}
{{ form_widget(form.Envoyer) }}
{{ form_end(form) }}
    </div>
{% endblock %}